# 待办清单（第二轮）—— 消融机制对齐评估与 Tent 独立链修复

> 创建：2026-08-11。上一轮清单（`待办问题与解决方案清单.md`，问题 1–14）已全部执行完毕
> 并随 commit 1d33c4f 推送。本清单记录新一轮讨论产生的待办事项，定案后统一执行。
> 状态：`待讨论` / `方案已定-待执行` / `已完成`。

---

## 问题 15：消融实验增加"机制对齐"评估维度（指标预注册）

- **动机**：现有消融用同一终点指标 F 评价机制不同的三组件（Tent 作用于第 0 代、
  Levy 作用于停滞时刻、DE 作用于每代），只能回答"有没有用"，不能回答
  "在其声称的机制上是否有效"。需为每个组件配置机制对齐指标，将消融从
  "打分"升级为"归因"。
- **指标定义（预注册，先定义后测量，全部变体统一计算，禁止事后挑选）**：

  **① Tent —— 初始化质量/多样性（只看第 0 代）**
  | 指标 | 定义 |
  |---|---|
  | 初始种群质量 | tent 初始化后 vs 伪随机基线的 init minF / meanF |
  | 初始联合多样性 | 个体到种群质心的平均距离（暴露单链同轨道坍塌） |
  | 温启动等效代数 | 随机初始化的主循环追平 tent 起点所需代数 |

  **② Levy —— 跳出局部最优（只看停滞事件）**
  | 指标 | 定义 |
  |---|---|
  | 停滞逃逸率 | 停滞 = best F 连续 20 代无改进；停滞期内由 Levy 阶段打破的比例（对照无 Levy 变体的自然逃逸率） |
  | 停滞时长分布 | 有/无 Levy 的平均与最长停滞代数 |
  | Levy 接受时机分布 | 接受事件在早/中/晚期（0-1/3, 1/3-2/3, 2/3-1）的占比 |

  **③ DE —— 搜索/开发效率（看全程）**
  | 指标 | 定义 |
  |---|---|
  | 每千次求值改进率 | ΔF / kNFE（NFE 归一，消除各变体每代求值数差异） |
  | time-to-target | 达到 V1 中位曲线终值所需代数（表 A2 已有） |
  | 尾程精修量 | 最后 100 代的 F 改进量 |

  **④ 贯穿指标（裁决交互效应）**
  | 指标 | 定义 |
  |---|---|
  | 阶段贡献分解 | 插桩记录主运动/Levy/DE 各阶段的 ΔF 贡献与接受数（"每一分改进是谁挣的"） |
  | 多样性轨迹 | 种群质心距离随代数变化曲线（可视化均质化/良种垄断停滞） |

- **实现**：`algorithms.run()` 加可选 `track=True`（阶段 ΔF/接受数 + 每代多样性，
  质心距离 O(pop·dim)/代）；消融新增 表 A3（机制对齐指标汇总）与
  图 A4（阶段贡献分解）、图 A5（多样性轨迹）、图 A6（停滞逃逸对比）。
  终点 F 统计维持 30 种子；轨迹类插桩用 10 种子即可。
- **方法学声明（写入论文）**：机制指标为解释层，不替代表 A2 的终点 F 主结论；
  指标于运行前预注册（即本清单）。
- **状态：方案已定-待执行**（与问题 16 同批重跑）。

---

## 问题 16：Tent 改为"每个体独立混沌链"（承接上轮问题 14 的可选后续①）

- **动机**：burn 对照实验已证明现单链实现的 −5.5% 来自"61% 个体被替换为同一条
  确定性轨道上的点"的联合多样性坍塌（V2-burn vs V2：p=0.019；RNG 扰动无影响
  p=0.59）。文献标准做法是每个体独立链。
- **方案**：`algorithms.run()` tent 阶段改为每个体独立 r0、各自迭代 k=10 次
  （μ=1.99 不变）；已实测独立链初始化 minF 34.06 优于伪随机 35.43。
- **预期**：V2 从 −5.5% 回归中性偏正；**V5 终点性能预计不变**（V5=V8=0.7001，
  DE 主导下初始化效应在 500 代内被冲刷）——不做性能期待，收益在消融公平性。
- **状态：方案已定-待执行**（与问题 15 同批）。

---

## 问题 17（可选，待讨论）：温启动作为独立消融维度

- **动机**：旧 Tent bug 的实质是"平行地面温启动良种"。本轮已证实温启动在
  有限预算下存在三种情形（帮助/无影响/有害，见上轮清单问题 14 与 P6 证据）。
- **方案**：贴地线（x=0.5，F≈2.8）显式注入初始种群作为"工程先验温启动"消融维度
  （对全部变体共享保证公平）；论文记为领域知识贡献而非混沌初始化贡献。
  可产出"温启动 × 算法强度"的条件性结论（弱搜索器受益大、强搜索器无感、
  缺多样性管理时可能锁死）。
- **状态：待讨论**（若采纳，与问题 15/16 同批，边际机时≈0）。

---

## 问题 18：多算法 P4–P6 上 GWO 优于 IJS 的机理分析（2026-08-11，已完成分析，供论文撰写）

- **数据**：P4 IJS 4.2 < GWO 5.1（IJS 胜）；P5 30.8 vs 12.2、P6 323 vs 34（GWO 胜）；
  GWO 仅用 IJS 1/3 求值预算与 1/6 运行时间。收敛形态：IJS"快启动+爬行"，
  GWO"蛰伏+断崖"（a 线性退火至临界值后全维同步几何收缩）。
- **机理**：P5/P6 的 F 由分段线性凸的坡差惩罚主导（占 94–98%），问题近凸——
  最优策略是退火式向最优点收缩（GWO 结构）；IJS 无步长退火、收缩锚点为种群均值，
  插桩显示 P6 上 120 代多样性即塌缩至 0.09 而 bestF 仍 620（早熟抱团后差分步长
  随离散度同步萎缩，进入爬行）。接受率未塌（main 44%/DE 18%），瓶颈是单步改进量。
- **历史版本 IJS "总是最好"的两个原因**：①早期口径（路面当地面+坡差限值不随
  步长归一）使高维问题温和，DE 精细重组占优；②上一批 Tent 塌缩 bug 白送
  全 0 良种（IJS P6 初代 43.7，500 代仅自行改进 1.7%），掩盖高维修复弱点——
  同批 GWO 从 5015 爬到 40.8 实际已更强。
- **论文表述**：工程尺度 P1–P4 IJS Friedman 第一（覆盖论文应用口径），
  P5/P6 为超高维约束修复压力测试、GWO 的退火领导收缩更适配（如实报告+机理解释）；
  IJS 加步长退火/最优锚定收缩留作展望。
- **状态：分析已完成，无需重跑；表述待论文撰写。**

---

## 问题 19：多算法对比升级为"平纵联合规模阶梯"PJ1–PJ6（替代原单点 PJ 方案）

- **动机**：P1–P6 均为固定平面的纵断面子问题（规模阶梯只能用纵断面步长生成:
  平面控制点的横向自由度随步长平方缩放 A_max=Δs²/(π²·R_min), Δs=10m 时仅 2.5cm,
  故平面无法做步长阶梯——属实验设计的有意简化, 非论文缺陷）。但由此缺少
  "六算法在最终联合问题上的直接对比"证据。2026-08-11 讨论升级：单点 PJ →
  六规模联合阶梯，规模变量仅取纵断面步长（平面固定 50 模态，避免双变量混淆）。
- **方案**：
  - 步长阶梯 500/400/300/200/100/50 m（PJ1–PJ6）：

    | 规模点 | 步长 | 纵断面维度 | 总 dim |
    |---|---|---|---|
    | PJ1 | 500 m | ~45 | ~95 |
    | PJ2 | 400 m | ~56 | ~106 |
    | PJ3 | 300 m | ~75 | ~125 |
    | PJ4 | 200 m | ~112 | ~162 |
    | PJ5 | 100 m | 225 | 275（=主实验口径） |
    | PJ6 | 50 m | ~449 | ~499 |

  - 均 ±500m 走廊带, dg_lim 按步长归一（3e-4×Δs）。
  - 6 算法 × 10 种子 × 6 规模 = 360 runs（声明减种子原因: 联合求值成本高）;
    NFE 与主实验对齐（pop200×iter500=1e5）; NSGA-II 用联合双目标。
  - 报告 mean/median F、Wilcoxon; **加分项**：借问题 15 插桩把收敛拆为
    "进入可行域代数 + 可行域内 ΔF" 两段报告，展示 IJS 精调期机制优势。
- **预期（诚实预登记，避免事后叙事）**：
  - 大概率胜 JS/PSO/WOA/GA；对 GWO 不保证全胜。机制依据：罚下降期（凸罚地形）
    利 GWO 退火收缩（问题 18 结论）；可行域内精调期（桥隧替换不连续+平纵耦合多峰）
    利 DE 重组+Levy 逃逸。主实验已证 IJS 在 dim=275、1e5 NFE 内稳定 pen=0，
    即 PJ5 预算足以进入第二阶段。
  - 分规模：PJ1–PJ2 低维差距小（类 P1–P2）；PJ3–PJ4 IJS 与 GWO 约五五；
    PJ5 IJS 胜出概率最高（估 60–70%），且 mean/std 稳定性应占优（消融 V5
    std 最小、conv~186 代）；PJ6（dim≈499）超主实验规模，若 1e5 NFE 内
    进不了可行域则重现 P4–P6 的 GWO 优势——该点是对"IJS 适用规模上界"的
    诚实压力测试，无论胜负都有论文价值。
- **机时**：约 2–4 min/run（敏感性 reopt 实测 ~2min/run；PJ6 站点数翻倍，
  单次求值约 2× 计入），360 runs 并行 32 核约 10–16h
  （可与问题 15/16 消融批次错峰, 合计控制在 32 核内）。
- **状态：方案已定-待执行**。

---

## 问题 20：主实验 Pareto 前沿与论文图 6.7 差距悬殊的归因（论文硬伤为主，口径差异为次）

- **现象**：我们前沿 C 20.2–56.9亿 / E 7.4–13.9亿；论文图 6.7 纵轴标"千万"
  刻度到 1000（=100亿），横轴能耗 5–40亿，跨度大得多且形状为长尾 L 形。
- **论文自身问题（证据）**：
  1. 决策点落不到自己曲线上：正文 E=8.50亿/C=9.64亿=96.4千万，但图上
     x=8.5 处曲线≈950千万≈95亿，差 10 倍；若纵轴实为"百万"则 (8.5,964百万)
     恰为左上端点——单位标注必有一处错（"千万"疑为"百万"误标，但那样
     尾部 LCC 仅 0.6亿/20.8km 亦不可能）。
  2. 表 6.8 土方工程 CTU=652130 元（20.8km 仅 65 万）数量级错误，且
     优化方案 CTU 反而大于现状。
  3. 绝对造价失真：9.64亿/20.8km≈0.46亿/km，与其表 6.6 单价、广州官方
     指引（高架 1.56亿/km）、北环实际造价差约 3–4 倍。
  4. 熵权决策点取前沿极端端点而非 knee——与我们已修复的"极差归一化
     偏向端点/全线高架"病态同源（本仓库以预算约束 C≤1.1×现状修复）。
- **口径差异（合理来源，非坐标错误）**：单位双方均为亿元；我们 CB 立交
  7.8km×1.56亿=12.2亿为常数项抬高 C 下限、压扁相对跨度；±500m 走廊带+
  预算约束剔除极端方案（sweep 中 w1≤0.15 的 C≈56亿 点即被排除出决策）。
  E 数量级与论文一致（8.5 vs 12.9亿）。趋势方向（C–E 负相关权衡）一致。
- **结论**：不复现论文绝对数值；论文撰写时说明图 6.7 单位标注矛盾与
  造价失真，以官方造价口径重建的前沿为准。
- **附带清理项**：`优化方案对比/results/front_ewm_selected.json`（2026-08-09,
  C=13.04亿）为修复批次前陈旧产物（最终决策以 2026-08-10 joint_results 为准,
  图 C1/C8 不受影响），建议删除或移入 archive。
- **状态：分析已完成；清理项待执行**。

---

## 问题 21：CB 从"7.8km 常数"升级为几何内生触发的交叉桥模型（2026-08-11 讨论定案）

- **动机**：已有 OSM（road/rail/water 三类障碍物折线, 已剔北环自身）与 DEM 后，
  桥梁不应再按人工统计常数累加。平面线形与障碍物在笛卡尔坐标系相交即触发桥，
  桥长由交叉区间几何决定——平面参数由此真正参与桥梁成本与净空约束（3D 耦合）。
- **触发规则（已定）**：
  1. 触发类别：road ∈ {motorway, trunk, primary} + rail + water；
     **secondary 不触发**（现实为下穿通道/涵洞，属路基附属造价，声明忽略）。
  2. 交叉检测：候选折线（225 段@100m）× 障碍物折线线段求交；
     障碍物一次性预建空间网格索引（单次评估增量 ~1–2ms）。
  3. 桥区间长度 = 障碍物缺省宽度 / sinθ（θ=交叉角）+ 两侧各 E_ext；
     缺省宽度表：motorway 40m / trunk 30m / primary 25m / rail 每股道群 10–15m /
     water 按涌/河分级 15–60m（OSM width 标签稀少，不补拉 riverbank 面数据）。
  4. E_ext 默认每侧 75m，参数化并纳入敏感性（扫 50/75/100m）。
  5. 区间合并：间隙 <100m 合并为连续高架（消除双向车道/平行 way 重复计数）。
- **成本与土方衔接（已定）**：
  - CB = 合并后 L_bridge × 1.56 亿/km（官方指引价）+ 7 处立交节点**匝道常数**另计
    （现状方案同规则重评，保持功能性成本口径一致）。
  - 触发区间动态豁免土方（替代现固定豁免带）；30m 填方换桥准则仅在触发区间外
    生效（防双计）；生态隧道区优先级高于桥。
- **新增约束（已定，全部纳入）**：
  - 净空罚项：交叉桩号处 design_z − 障碍物地面高程 ≥ H_clear
    （跨公路 5.5m；跨电气化铁路 ≥7.96m——京广/广深线；跨河 ≥ 岸面+安全值），
    归一化后与 dg 罚同栈。
  - 斜交角：θ<30° 设罚；θ 适中靠 1/sinθ 成本自然惩罚。
- **校核门槛（重跑前置条件）**：现状线形按同规则触发，结果须对上实际北环
  （7 处立交 + 京广线 + 石井河等，总桥长与 7.8km 可比）；同步产出新 C_ref
  与预算约束。校核不过不得重跑。
- **影响面**：主实验 + 敏感性需重跑；消融/多算法用固定平面
  （fixed_plane_ctx 中交叉一次性预计算，近零成本）。改动文件：
  objective_joint.py / objective.py / params.py（宽度表与 H_clear）/ run_joint.py。
- **状态：方案已定-待执行**。

---

## 执行批次规划

| 项目 | 改动 | 机时 |
|---|---|---|
| 问题 21（交叉桥内生模型）——**先行批**，改变联合目标函数口径 | objective_joint.py / objective.py / params.py / run_joint.py + 校核脚本 | 实现+校核 ~0.5 天；主实验重跑 ~6–8h |
| 问题 15（机制指标插桩+图表） + 问题 16（独立链） | algorithms.py / run_ablation.py / make_outputs.py | 一次消融重跑 ~8–9h（30 种子终点 + 10 种子轨迹） |
| 问题 17（温启动维度，若采纳） | 并入同批 | +0h |
| 问题 19（PJ1–PJ6 联合规模阶梯，步长 500/400/300/200/100/50m）——**须在 21 之后**（联合目标口径变更） | run_comparison.py / make_outputs.py（多算法） | 360 runs 并行 ~10–16h，可与消融批错峰 |
| 敏感性重跑（随 21 口径 + E_ext 敏感性项） | run_reopt.py / make_outputs.py | ~6–10h |

> 执行顺序：**21 →（校核通过）→ 主实验 → {15+16 消融批 ‖ 19 多算法批 ‖ 敏感性批}**。
> 注意：问题 16 改 `algorithms.py` 的 tent 实现，**主实验/多算法/敏感性的 IJS 也会
> 受影响**（tent 效应本就被 DE 冲刷，预计数值变化在噪声内）；为保持全文一致，
> 建议消融重跑确认 V5 无实质变化后，再决定其余三实验是否需要同步重跑
> （若 V5 变化 <1% 可声明不重跑）。
