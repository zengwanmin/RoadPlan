# 待办问题与解决方案清单

> 用途：记录当前已发现、暂缓修改的问题及候选方案。所有问题讨论定案后**一次性统一修改并重跑**，
> 避免多轮零碎重跑浪费机时。每项标注影响范围与预估机时。
> 状态：`待讨论` / `方案已定-待执行` / `已完成`。
> 创建：2026-08-10（消融实验数据分析后）。持续更新。

---

## 一、消融实验相关

### 问题 1：收敛代数指标存在"相对自身"伪象 —— V5(448代) 看似比 JS(318代) 收敛更慢

- **证据**：`conv_gen` 定义为完成**自身**总改进 99% 所需代数。V5 挖得更深（f*=0.80 vs 1.13）
  且 250 代后仍收获 14.3% 的改进（V1 仅 1.5%，属早熟停滞）；Tent 又把 V2/V5 的曲线起点
  从 f0≈40 拉低到 3.28，同一 99% 阈值在小改进区间上天然更晚达标。两组变体的 conv_gen 不可比。
- **客观口径反证**：time-to-target（达到 JS 最终值 F=1.125 所需代数）：V4=225，**V5=263**，
  V2=390，V3=491，V1=498 —— IJS 实际是 JS 的 1.9 倍速。
- **方案（已定）**：表 A2 增加两列——time-to-target、固定预算 F@100/F@300；
  原"99% 自身收敛代数"保留并加注含义说明。
- **影响范围**：`消融实验/make_outputs.py`（读取已存曲线即可，**无需重跑寻优**）。
- **机时**：<0.5h。**状态：已完成（2026-08-11）**。

### 问题 2：Levy 单独消融(V3)贡献为负(−0.6%)，能否转正

- **证据**：V3 vs V1 的 Wilcoxon p=0.853（统计上无差异，−0.6% 是噪声）；按中位数 V3 反而 +0.7%。
  根因是 **Levy 步长未按搜索域缩放**：每维步长中位 0.255（≈种群个体间距 0.292），P90=1.31
  超过整个 [0,1] 定义域，插桩实测接受率仅 **0.48%**（主运动 45.2%、DE 16.3%）——Levy 阶段
  近乎死重，每代还多烧 1×pop 次求值。
- **候选方案**：
  - **A（零成本，报告口径）**：表 A2 增加中位数列与 Wilcoxon p 值列；文字表述改为
    "Levy 单独中性（p=0.853），与 DE/Tent 组合后显著（V4→V5 −2.26%，p=0.035）"。
  - **B（根治，实现修复）**：Levy 步长按标准做法缩放（`0.01·(ub−lb)·levy` 或
    `levy⊙(xᵢ−x_best)` 朝最优引导，Yang & Deb 布谷鸟搜索口径）。预期 V3 真实转正、
    V5 提速。**改 `algorithms.py` 牵动全部实验**（IJS 处处在用）。
- **影响范围**：A 仅消融图表；B 需重跑 消融+多算法+主实验+敏感性 ≈ **20h 机时**。
- **状态：已完成（2026-08-11，A+B 均执行；修复后 V3 +3.8%，p=0.02 显著转正）**。

### 问题 3：消融设计无法剥离 Levy 的组合贡献（V4→V5 的增量混合了 Tent+Levy）

- **方案（建议）**：升级为 **2³ 全因子设计**：在 V1–V5 基础上补 V-TL(Tent+Levy)、
  V-TD(Tent+DE)、V-LD(Levy+DE)，共 8 变体；可做主效应+两两交互效应分解。
  **保留 V3**（全因子必要组成，且"单独中性、组合显效"如实报告是加分项）。
- **影响范围**：`消融实验/run_ablation.py` VARIANTS 扩展 + make_outputs 适配。
- **机时**：+3 变体 × 30 次 ≈ **3.5h**。**状态：已完成（2026-08-11，V6/V7/V8 已入全因子）**。

### 问题 4：各变体每代函数求值次数(NFE)不同，per-iteration 比较对多求值变体有利

- **证据**：V1/V2=1×pop/代，V3/V4=2×，V5=3×。500 代总 NFE：100k/200k/300k。
  等 NFE 下 V4(0.8189@200k) 与 V5(0.8004@300k) 的差距会缩小。
- **方案（建议）**：表 A2 增加"每代 NFE"列并在正文声明；可选补一组等 NFE 对照
  （如 V4 跑 750 代）。
- **机时**：声明 0h。**状态：已完成（表 A2 增加每代 NFE 列；等 NFE 对照未做，如需另议）**。

---

## 二、主实验/模型口径相关

### 问题 5：±250m 情景白云山段最大挖深 37.7m 超过论文 30m 隧道判据（判据缝隙）

- **证据**：结构替代封顶 min(土方费, 隧道价) 的成本交叉点在挖深≈69m，30–69m 区间的
  深挖按土方计费，与论文 §3.4.2 的 30m 几何判据存在缝隙。主实验 ±500m 方案（24.2m）不受影响。
- **候选方案**：①论文中声明该段宜按短隧道/棚洞处理并近似计费；
  ②把生态区外挖方的结构替代从"成本交叉"改为"30m 硬判据"（挖深>30m 即按隧道价计费）并重跑。
- **影响范围**：②改 `objective.earthwork_cost` + 全部实验重跑。**状态：已完成（2026-08-11，采纳②30m 硬判据）**。

### 问题 6：主实验西端终点附近存在 44.2m 深挖段（约 300m，浔峰洲丘陵区）

- **证据**：联合 M-C 全线最大挖深 44.2m 出现在里程 22.26km 附近，同样落入 30–69m 判据缝隙。
- **候选方案**：与问题 5 合并处理（硬判据会自动把该段转为隧道计费）；或结论中单独注明。
- **状态：已完成（随问题 5 的 30m 硬判据自动处理）**。

### 问题 7：桥梁单价 886.69 万/km 的原始出处待核实

- **现状**：已判定有误弃用（与广州官方指引 15.6 万/延米差 17 倍），当前用 1.56 亿/km
  （官方下限，来源存 `数据/造价依据/`）。统计表中该数字的来历（漏乘车道数/单幅/口径混淆）
  建议向数据提供方核实一次，论文中如需解释可写"经官方指标校核修正"。
- **状态：待用户核实**（不阻塞实验）。

### 问题 8：白云山隧道段 GPS 高程漂移（洞内无信号，Altitude 为设备外插值）

- **证据**：现状线 10.4–10.8km 段"路面高程"高出准天然地面 7–17m（物理不可能），
  影响现状方案 M-A 在该段的土方/能耗计量精度（量级小，因该段多在生态区豁免带内）。
- **候选方案**：洞口两端高程线性内插替换洞内 GPS 值（`data_loader` 预处理，
  桩号区间由生态区掩膜自动确定）。
- **影响范围**：M-A 基线数值微变。**状态：已完成（2026-08-11，洞口线性内插，随统一批次重跑）**。

---

## 三、多算法对比相关

### 问题 9：P6（dim=2247）上 GWO(40.94) 略优于 IJS(43.07)

- **现状**：P1–P5 IJS 全部 Friedman 第一（p≈1e-29），仅超高维 P6 被 GWO 反超。
- **方案（建议）**：如实报告；正文说明工程口径以 P3（100m 变坡点）为准、P5/P6 为规模
  压力测试；可讨论 GWO 头狼引导在超高维的优势作为 future work。
  若执行问题 2 方案 B（Levy 修复），P6 结果可能改善，届时重新评估。
- **状态：结论更新（2026-08-11）：修复 Tent bug 后 P5/P6 由 GWO 领先（IJS P6 从 43 回落至 310），按问题 11 两段式表述如实报告。**

### 问题 10：Tent 混沌映射存在浮点塌缩 bug —— IJS 的"Tent 初始化"实为"注入一个全 0 向量"

- **证据（插桩实测）**：μ=2 的 tent 映射在 float64 下二进制尾数逐位左移耗尽，
  **迭代 54 次后混沌序列塌缩为全 0 并永久停留**（x<0.5 时 2x 等价二进制左移，
  0.5 的精确表示使轨道落入 0 不动点）。`algorithms.run` 的 tent 阶段对 200 个个体
  逐个迭代同一条链 → 第 54 个个体起全部候选 = 全 0 向量，仅前 53 个个体收到真混沌扰动。
- **连带效应**：全 0 向量解码 = "整体下移 amp 的平滑地面线"，恰是一个天然低惩罚的
  高质量起点（P6: F=43.74 vs 随机种群 F≈5015）——**IJS 初代优势 99% 来自这一个
  意外注入的良种，而非混沌遍历性**。μ=2 是文献常见取值但必须配合扰动或改用
  μ=1.99/加微噪声避免精确二进制退化。
- **候选方案**：①μ=2→1.99 或 tent 输出叠加 1e-12 量级抖动；②tent 链条按个体独立
  初始化（避免单链塌缩传染全种群）。均改 `algorithms.py`，与问题 2B 同批重跑。
- **状态：已完成（2026-08-11，μ=1.99，插桩验证 200 次迭代无塌缩）**。

### 问题 11：P4–P6 的标量目标 F 被惩罚项主导，实为"约束满足能力"而非"成本-能耗优化能力"的比较

- **证据**：可行解 F≈0.7–0.8，而各规模 IJS 最终 meanF：P4=3.75、P5=14.5、P6=43.1 ——
  惩罚占比约 79%/94%/98%，**P4 起所有算法都未进入可行域**（坡差约束 3×10⁻⁴/m 在
  高维下随机剖面的违反量随 dim 增长）。P4–P6 排名反映的是高维约束修复速度。
- **方案（建议）**：论文明确两段表述——P1–P3 比较"可行域内优化质量"，
  P4–P6 比较"高维约束满足/修复能力"（规模压力测试）；或对 P4–P6 引入可行性
  优先的字典序指标。**状态：已定案（论文按两段式表述，代码不改）**。

### 问题 12：图 B1 中 IJS 初代 F 远低于其他算法，系"初代值含 Tent 阶段"的口径差异

- **证据**：所有算法共享同一初始种群（rng(1000+r)），但 IJS 的 curve[0] 记录的是
  tent 阶段完成后的最优值（P6: 43.7 vs 其他算法 5014.9）——图面上"IJS 赢在起跑线"
  实为问题 10 的全 0 良种注入 + 记录口径差异，非初始种群不公。
- **方案（建议）**：图 B1 注明"IJS 初代含混沌初始化改进"；或曲线增加 tent 前的
  原始初值点。**状态：已定案（图注声明；Tent 修复后初代差异已大幅缩小）**。

---

## 三之二、敏感性分析相关

### 问题 13：图 D1–D5 混乱不可读 —— 每点独立熵权 + 退化解切换 + 单种子寻优噪声三重污染

- **证据（对 reopt_results.json 的定量诊断）**：
  - item1（交通量×EV，126 点）：每点各自的熵权 wC 在 **0.181–0.565 间漂移**（极差 0.384）；
    **34/126 个点落入"全线高架"退化解**（C>40 亿）；wC 与 C 的相关系数 **−0.63**
    （C 的起伏主要由权重决定，不是由参数决定）；相邻 EV 点（Δ仅5%）之间 C 平均跳动
    **11.7 亿**；E 随 EV 的单调性破坏率 29%。
  - item2/item3：C 退化点 15/36、5/36；E 行内单调性破坏率 37%、47%。
  - item4（D4 前沿）：w1=0.3→0.4 处 C 从 53.9 亿断崖跳到 23.7 亿（解族切换），9 点中 3 点被支配。
  - **对照组证明根因**：固定权重的 D6–D8 扫描无一退化点（C 全部 20.5–22.9 亿），
    仅剩 ±1 亿量级的单种子寻优纹波——与 D9 走廊带修复（固定 w1=0.65）结论一致。
- **机理**：①每点重算熵权使"决策权重"成为随参数漂移的隐藏自变量，权重越过临界值后
  解族在"贴地"与"高架"间切换，响应面出现断崖；②每点单种子单次寻优（500 代、
  无两阶段惩罚收紧），run-to-run 方差约 1 亿，而参数真实效应仅 0.2–0.5 亿/步，信噪比<1；
  ③C 对 EV/油价/节能率只有二阶通道（经权衡点移动），D1(a) 的 C 面与 D5 的 L/Rmin 面
  在当前噪声下基本是纯噪声图。
- **候选方案**：
  - **A（推荐，重跑敏感性 ~4h）**：item1–3 全部改为**固定决策权重 w1=0.65**
    （与主实验决策点、D9 口径统一）；D4 保留权重扫描本职并叠加预算约束筛选；
    D5 改为展示"E 面 + 固定权重下的真实二阶效应"或删除。
  - **B（增强，机时×3）**：在 A 基础上每点 3 种子取中位，消除寻优纹波。
  - **C（零重跑，应急）**：现有数据筛除 C>40 亿退化点重画 + 图注声明；
    D1(a)/D5 的 C/L/Rmin 面删除（纯噪声），只保留 E 面。
- **状态：已完成（2026-08-11，方案 A；验证退化点 34→0、跳动 11.7→0.09 亿、E 单调破坏 29%→0%）**。

---

## 四、汇总：统一重跑批次规划（定案后执行）

| 若采纳 | 需重跑 | 机时 |
|---|---|---|
| 问题 13A（敏感性固定权重） | 仅敏感性实验 | ~4h（独立批次） |
| 问题 1（指标列）+ 问题 2A（报告口径）+ 问题 4（NFE 声明）+ 问题 9（表述） | 仅重新出图表 | <1h |
| 问题 3（全因子消融） | 消融 +3 变体 | +3.5h |
| 问题 2B（Levy 缩放修复） | 全部四实验 | ~20h |
| 问题 5/6（30m 硬判据） | 全部四实验 | ~20h（可与 2B 同批） |
| 问题 8（隧道段高程修复） | 全部四实验 | ~20h（可与 2B/5 同批） |

> 注：2B、5/6、8 三项都动底层口径，**务必合并为同一批次重跑**，只付一次 20h 机时。
> 全部定案前不做任何底层修改，保持当前已提交版本（9fa8f99）可复现。

---

## 五、已完成事项存档（本轮已解决，无需再议）

1. 准天然地面重建（现状土方沉没成本归零问题）——已实现并验证。
2. CB 空间锚定：立交 OSM 锚定常数计费 + 生态区内生隧道——已实现，
   镜像错位 bug 已修复（数据.xlsx 里程零点在东端）。
3. 桥梁单价 1.56 亿/km（广州官方指引）——已实现。
4. 前沿熵权决策（预算约束 C≤1.1×现状）——已实现，替代初始种群熵权标量单点。
5. 平面 50 模态 + 纵断面 100m（dim=275）、走廊带 ±500m 主口径——已定案。
6. 敏感性 scenario 注入（油电价逐年增长×折现）、走廊带敏感性 D9（固定 w1=0.65）——已实现。
7. 竖曲线坡差限值按桩号步长归一（3×10⁻⁴/m）——已实现（多算法 P1–P6 口径公平）。
8. 四实验口径文件统一同步机制——已实现（以主实验目录为准单向同步）。
