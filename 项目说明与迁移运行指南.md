# 项目说明与迁移运行指南

高速公路立体线形优化实验（IJS + 熵权法）。本文档用于把项目迁移到另一台设备后**快速搭好环境并运行**，含尚未跑过的实验的启动方法与预估耗时。

---

## 一、项目概述

以广州北环高速实测数据为例，用改进水母搜索算法（IJS）+ 熵权法标量化，做高速公路平纵断面立体线形优化。共 4 个实验，各在独立文件夹：

| 实验 | 文件夹 | 主运行脚本 | 作用 |
|---|---|---|---|
| 消融实验 | `消融实验/` | `run_ablation.py` | IJS 三改进组件（Tent/Levy/DE）消融，5 变体×30 次 |
| 多算法对比 | `多算法对比/` | `run_comparison.py` | IJS vs NSGA-II/GA/PSO/GWO/JS，**6 规模**×30 次 |
| 优化方案对比（平纵联合协同） | `优化方案对比（平面、纵断面联合协同优化）/` | `run_joint.py`、`run_twostage.py` | 平纵三维联合协同优化；两阶段对照 |
| 敏感性分析（平纵联合，重优化） | `敏感性分析（平、纵联合，重优化）/` | `run_reopt.py` | 每采样点重新优化线形的敏感性分析 |

`数据/` 为共享数据源；`实验设计方案2（…）.docx` 为总体设计说明。

**四实验统一口径**（已对齐，迁移后无需改）：
- 数据源：各实验 `data_loader.py` 均指向 `实验/数据/数据.xlsx`（相对本文件夹上一级定位，迁移不受绝对路径影响）。
- 桥隧：桥梁 7.8 km、隧道 2.7 km；桥 886.69 万元/km、隧 27000 万元/km；基准里程 L_ref=22.342811 km（取自 `数据/北环高速现状桥梁隧道统计.xlsx`）。
- 能耗：全生命周期货币量（亿元）＝单日能耗 × 365 × 30 年 5% 等额年金现值，不乘标定系数。
- 成本、能耗单位均为**亿元**。
- **联合模型离散尺度（决策变量与评价网格分开）**：决策变量为平面 57 个控制点法向偏移 + 纵断面 57 个变坡点高程（均 400 m 一个，`STEP_CTRL_M`），`dim=114`；目标函数评价仍在 2247 个 10 m 桩号（`STEP_EVAL_M`）上积分。地面高程按里程比例（`np.interp`）取实测中线对应点，不用 `cKDTree` 最近邻（后者平面大幅侧移后会失效，详见 `运行记录与问题定位.md` §3.2）。实验③与④共用**同一份** `objective_joint.py`（两目录文件内容完全一致，可用 `md5sum */objective_joint.py` 核对）。
- **CQ 边坡养护项**按沿里程积分的面积口径，与桩号步长无关，使实验②跨 6 种步长的 C/F 可横向比较。

> **决策变量取 400 m 的理由**（务必保留，否则平面/纵断面优化会退化）：
> 1. 平面受平曲线最小半径 R ≥ 400 m 约束，相邻控制点横向错动 A 与间距 Δs 满足
>    `R = (2Δs)²/(4π²A)`。Δs=10 m 时只允许 A ≤ 2.5 cm，而走廊带是 ±800 m ——
>    搜索空间几乎全域不可行，实测中线自身在 10 m 控制点下 Rmin 也仅 53.5 m
>    （拟合的是 GPS 噪声）。Δs=400 m 时实测中线基线 Rmin=525.8 m 本身合规，
>    相邻控制点可差约 40 m，平面才具备真实选线自由度。
> 2. 纵断面变坡点也必须取 400 m（而非曾用过的 10 m/2247 个）：若与平面的 57 维
>    放进同一决策向量，2247 维纵断面会把 57 维平面彻底淹没——IJS 按整体贪婪接受，
>    "改善平面但恶化纵断面"的候选被拒绝，纵断面在贴地附近已近最优、任何扰动都变差，
>    平面因而无法取得进展。实测：57+2247 时联合优化里程仅缩短 0.01%（δ≈0，平面未动），
>    末 100 代 F 仅改善 1.7e-4（结构性停滞）；调平到 57+57 后立刻 −2.6%，
>    约束惩罚从 1.66e8 降到 2.16e4。目标函数的评价精度不受影响：变坡点高程仍线性
>    插值到 2247 个 10 m 桩号上积分。详见 `运行记录与问题定位.md` §3.3-3.4、§六。

---

## 二、环境搭建（迁移第一步）

1. 安装 Python 3.10+（开发用 3.13，已在 3.10.12 上验证通过）。
2. 在 `实验/` 目录下安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
   依赖：numpy、scipy、pandas、matplotlib、openpyxl（读 xlsx）、python-docx（可选，仅生成 docx 用）。

   **`openpyxl` 是硬依赖**：`pandas.read_excel` 少了它会在 `load_alignment()` 第一步直接抛 `ImportError: Missing optional dependency 'openpyxl'`，四个实验全部无法启动。装完先自检：
   ```bash
   cd 消融实验/ && python3 data_loader.py
   # 期望输出: 样本点数 N = 14018 / 路段总长 = 22.462 km / 重采样桩号数 = 225
   ```
3. （可选）安装 pandoc —— 仅当需要用 `build_docx.py` 把 md 分析文档转带高亮的 docx 时。绘图/计算不需要。
4. **确认真实可用核数**（决定并行度与总耗时，见 §四）：
   ```bash
   nproc                                       # 可能是宿主机核数, 不可信
   cat /sys/fs/cgroup/cpu.max                  # cgroup v2: "配额 周期"
   cat /sys/fs/cgroup/cpu/cpu.cfs_quota_us     # cgroup v1: 除以 cfs_period_us
   ```
   开发环境 `nproc=124` 但 cgroup 配额只有 **16 核**。超配额开进程只会时间片切换、反而更慢。

> 迁移要点：只要保持 `实验/` 文件夹整体结构不变（各实验文件夹 + `数据/` 在同一层），数据路径为相对定位，**换设备/换绝对路径都不用改代码**。

---

## 三、目录结构

```
实验/
├── 数据/                         # 共享数据源(勿改名/移动)
│   ├── 数据.xlsx                 # 北环高速GPS轨迹(14018点)
│   └── 北环高速现状桥梁隧道统计.xlsx
├── requirements.txt
├── 项目说明与迁移运行指南.md      # 本文件
├── 消融实验/
├── 多算法对比/
├── 优化方案对比（平面、纵断面联合协同优化）/
└── 敏感性分析（平、纵联合，重优化）/
```

每个实验文件夹的通用结构：
```
<实验>/
├── run_*.py          # 主运行脚本(生成 results/*.json)
├── make_outputs.py   # 由 results 生成 figures/ 与 tables/
├── params.py         # 参数(pop=200, iter=500, 桥隧/能耗口径)
├── objective*.py     # 目标函数(成本C/能耗E/约束)
├── algorithms.py     # IJS 及变体
├── data_loader.py    # 读 数据/数据.xlsx
├── README.md         # 该实验的说明
├── results/          # 运行输出的 json(迁移可保留或删除后重跑)
├── figures/  tables/ # 图表输出
└── <实验>说明与分析总结.md / .docx
```

---

## 四、各实验运行方法与预估耗时

> 所有脚本均在各自实验文件夹内运行（`cd` 进去即可），无需设环境变量。
> 统一参数: pop=200, iter=500, n_runs=30（消融/多算法）。
> 耗时同时给出**核·h（总计算量，与机器并行度无关）**和**墙钟（实测/按 16 核配额估）**。
> 四实验总计约 **35 核·h**，16 核下按 §五 分三阶段实测总墙钟约 **5.6 h**。
> 建议并发进程数按配额的 **~70%** 给（16 核 → ~11 进程），而非跑满。

### 4.1 消融实验

```bash
cd 消融实验/
python3 run_ablation.py            # 正式跑
python3 run_ablation.py --smoke    # 冒烟测试(iter=5, n_runs=3, 验证管线)
```

- 5 变体（JS / JS+Tent / JS+Levy / JS+DE / V5_IJS）× 30 次独立运行
- 维度: 纵断面 dim=225（100 m 步长）
- 计算量 ≈ **1.2 核·h**；**固定单进程串行**，墙钟 ≈ 1.2 h
- 输出: `results/ablation_results.json`（冒烟写 `..._smoke.json`，不覆盖正式结果）

> **为何不并行**：表A2 把"平均运行时间(s)"作为结果列上报，跨进程并行会让各变体耗时受争用干扰而不可比。总耗时仅 1.2 h，坚持串行。

跑完后生成图表:
```bash
python3 make_outputs.py
```

### 4.2 多算法对比

```bash
cd 多算法对比/
python3 run_comparison.py           # 按规模 6 进程并行(规模内部串行)
python3 run_comparison.py --smoke   # 冒烟(iter=5, n_runs=3, 仅 P1/P6)
python3 run_comparison.py --serial  # 强制单进程串行(~10 h, 供严格计时复核)
```

- 6 算法（IJS / JS / GA / PSO / GWO / NSGA-II）× **6 规模** × 30 次 + 各规模 11 点权重扫描 Pareto 前沿
- 6 规模由纵断面桩号步长控制维度：P1 500 m (dim=45) / P2 300 m (75) / P3 100 m (225) / P4 50 m (450) / P5 25 m (899) / P6 10 m (2247)
- 计算量 ≈ **10 核·h**；按规模 6 进程并行，墙钟 ≈ 3 h（受最慢的 P6 支配，单 P6 约 3 核·h）
- 输出: `results/comparison_results.json`

> **并行粒度只到"规模"**：表B1/B2 上报运行时间，故**规模内部的 6 算法 × 30 次必须串行**，使同一张表里算法之间耗时在同一条件下测得。只有 6 进程并发，争用可忽略。

```bash
python3 make_outputs.py
```

### 4.3 优化方案对比（平纵联合协同优化）

两个脚本互不依赖、可并行启动；但 `make_outputs.py` 的表 C3 需要**两者**的结果，故出图必须放在最后。

```bash
cd 优化方案对比（平面、纵断面联合协同优化）/

# 两阶段对照(平面单独 → 纵断面单独, 严格串联故单进程) 与 联合优化 可同时跑
# setsid+-u+</dev/null: 曾观察到普通 nohup ... & 的 run_twostage.py 无报错静默退出,
# 换此写法后未再复现, 建议保留
setsid nohup python3 -u run_twostage.py > twostage.log 2>&1 < /dev/null &   # -> twostage_results.json, 实测 8.9 min
setsid nohup python3 -u run_joint.py --workers 10 > joint.log 2>&1 < /dev/null &  # -> joint_results.json, 实测 17.3 min

# 两者都结束后再出图
python3 make_outputs.py
```

- 联合优化维度: **dim=114**（平面 N_CTRL=57 + 纵断面 M_PROF=57，均 @400 m 决策变量；
  目标函数仍在 M_EVAL=2247 个 @10 m 桩号上积分，见第一节「离散尺度」说明）
- pop=200, iter=500；`run_joint.py` 求解 M-B、M-C 与 Pareto 21 个权重点，共 23 次独立 IJS 寻优，已并行化
- 计算量(实测): run_joint ≈ **2.9 核·h**（10 进程, 墙钟 17.3 min）、run_twostage ≈ **0.15 核·h**（单进程, 墙钟 8.9 min）
- 两者并行启动时墙钟 ≈ run_joint 的 17.3 min
- 两脚本均支持 `--smoke`
- 此前(修复前) `dim=2304`、地面高程用 `cKDTree` 最近邻时 run_joint ≈16 核·h/墙钟~1.4h；
  两处修复(纵断面决策变量降到57个 + 改按里程取高程)后降至上述数值，且修复了平面
  被高维纵断面淹没、地面高程在大幅侧移后失真两个缺陷，见 `运行记录与问题定位.md`。

### 4.4 敏感性分析（平纵联合，重优化）—— 最慢

```bash
cd 敏感性分析（平、纵联合，重优化）/
python3 run_reopt.py                 # 正式全量(并行度默认 cpu_count-4)
python3 run_reopt.py --workers 9     # 显式指定并行度(按真实配额~70%给, 16核→9-11)
python3 run_reopt.py --smoke         # 冒烟测试(iter=5, 26 个点)
```

- 每个采样点都重新跑一次完整 IJS(pop200/iter500) 联合优化(dim=114)，共 226 个点
- 采样点 = 226（① 6×21=126, ② 6×6=36, ③ 6×6=36, ④ 9, ⑤⑥⑦=7+7+5=19）
- 计算量(实测) ≈ **21 核·h**（9 进程, 墙钟 140.2 min ≈ 2.3 h）；占四实验总量约 60%
  （修复前 `dim=2304` 时 ≈160 核·h/墙钟~10h，占 84%；两处修复后大幅下降）
- **并行度**：`N_WORKERS` 已改为 `os.cpu_count()-4` 自适应，并可用 `--workers` 覆盖。**务必按真实 cgroup 配额的 ~70% 给**，跑满或超配额只会因时间片切换而更慢
- 输出: `results/reopt_results.json`

```bash
python3 make_outputs.py
```

---

## 五、推荐执行顺序

约束有两条：**总并发进程数 ≈ 真实可用核数的 70%**（而非跑满），且**上报运行时间的
两个实验必须与其它实验隔离**。据此分三阶段（下列墙钟为实测值，16 核配额，~11 进程上限）：

```
Stage 1  多算法对比(6 进程) + 消融(1 进程串行)          墙钟 ~3 h     [必须隔离]
           ↓ make_outputs.py ×2
Stage 2  run_joint(10 进程) + run_twostage(1 进程)     墙钟 ~17.3 min
           ↓ make_outputs.py
Stage 3  run_reopt(9 进程)                             墙钟 ~140 min (~2.3 h)
           ↓ make_outputs.py
                                                       合计 ~5.6 h
```

Stage 1 之所以要隔离：消融（表A2）与多算法对比（表B1/B2）都把"平均运行时间"作为结果列，
此阶段只用 7 个进程、留有余量。Stage 2/3 修复前（`dim=2304`、`cKDTree` 最近邻）分别约
1.4h/10h，两处修复（决策变量降到114维 + 改按里程取高程）后大幅缩短，详见 `运行记录与
问题定位.md` §四、§六。

**一键编排**：仓库根目录的 `run_all_stages.sh` 实现了上述三阶段链（含每阶段自动出图）：

```bash
cd 消融实验/ && nohup python3 run_ablation.py > ablation.log 2>&1 & AB=$!
cd ../多算法对比/ && nohup python3 run_comparison.py > comparison.log 2>&1 & CP=$!
cd .. && nohup bash run_all_stages.sh $CP $AB > run_all_stages.log 2>&1 &
tail -f run_all_stages.log
```

**并行不改变数值结果**：每次运行的初始种群与随机种子均由 (规模, 算法, run 序号) 或 (采样点全局序号 `SEED_BASE+gid`) 唯一确定，与执行顺序、并行度无关。

---

## 六、冒烟测试（快速验证迁移是否成功）

迁移到新设备后，先用冒烟模式验证全管线无报错。**5 个入口脚本全部支持 `--smoke`**（`iter=5` + 少量采样点/规模，每个 1–2 min），结果写入 `results/*_smoke.json`，**不会覆盖正式结果**：

```bash
cd 消融实验/ && python3 run_ablation.py --smoke && cd ..
cd 多算法对比/ && python3 run_comparison.py --smoke && cd ..
cd 优化方案对比（平面、纵断面联合协同优化）/ && python3 run_twostage.py --smoke && python3 run_joint.py --smoke && cd ..
cd 敏感性分析（平、纵联合，重优化）/ && python3 run_reopt.py --smoke && cd ..
```

验证无 ImportError / FileNotFoundError / 数据维度不匹配后再正式跑。

> **冒烟输出不可用于任何结论。** `iter=5` 远未收敛，会出现两类看似"出错"但完全正常的现象：
> 1. **优化结果差于"不优化"的现状方案**（如里程变长、C 反而更大）—— 初始种群是走廊带内的随机线形，其目标值比现状基准差数个数量级，优化器需要大量迭代才能回到基准附近；
> 2. **各权重点给出完全相同的解**（M-B、M-C、Pareto 各点数值一致）—— 此时惩罚项（归一化后 ~10²）远大于加权目标项（~10⁰），所有权重都退化为"最小化惩罚"，只有迭代到惩罚接近 0 后权重才开始区分解。

---

## 七、注意事项

1. **macOS vs Linux**: 代码用 `multiprocessing`，macOS 默认 `spawn` 启动方式，Linux 为 `fork`。代码已兼容（`_init_worker` 设计），无需修改。各并行脚本已在 `import numpy` **之前**把 `OMP_NUM_THREADS` 等设为 1，避免 BLAS 线程超订。
2. **内存不是瓶颈**: 联合优化 pop=200 × dim=114 ≈ 0.2 MB/种群，16 进程并行也远低于 1 GB。真正的约束是 CPU 时间（约 35 核·h）。
3. **随机种子**: 固定种子 `SEED_BASE=20250722`，结果可复现，且与并行度无关。
4. **图表语言**: 图坐标轴/图例均为英文（SCI论文需要），表格含中文列标题。
5. **数据文件不能改名/移动**: `数据/数据.xlsx` 和 `北环高速现状桥梁隧道统计.xlsx` 必须在 `实验/数据/` 目录下。
6. **性能热点（已修复）**: 联合模型单次目标函数求值约 **0.8 ms**。曾用 `cKDTree.query` 查最近实测中线点取地面高程（占当时单次求值 7.6 ms 的 92%），但该近似在平面大幅侧移后失效（侧移中位 570 m 时可能取到实测路线上相距数百米的点，产生虚假陡坡，实测达 104%），已改为按里程比例 `np.interp` 取值——修正失效场景之外还快约 11 倍。详见 `运行记录与问题定位.md` §3.2、§六。
7. **模型的结构性特征（写分析文档时须知）**：设计高程定义为"地面高程 + 决策偏移"，故填挖高度与地面高程绝对值无关 —— **全生命周期成本 C 完全不依赖地面高程**，地面高程只通过 `diff(gz)`（地面纵坡）影响能耗 E 与纵坡约束。已用"地面整体抬高 100 m 后 C/E/惩罚变化恰为 0"验证。
8. **结果的量级会与旧版分析文档不同**: 统一口径（桥隧改按实测长度×分类单价、能耗取消反向标定 `k_E`、CQ 改面积口径、平纵步长解耦）后，`C_ref`、熵权 `wC/wE` 与 F 的绝对值都与旧 `results/` 及旧 `.md/.docx` 分析文档不一致。四份 `说明与分析总结.md/.docx` **需按新结果重写**，其中敏感性那份仍在引用早已移除的 `β=4.2948×10⁷` 与 `k_E=0.6792`，务必删除。

---

## 八、快速命令汇总

```bash
# === 一键环境 ===
pip install -r requirements.txt
cd 消融实验/ && python3 data_loader.py && cd ..     # 自检: 应输出 N=14018 / 22.462 km

# === 确认真实可用核数(决定并行度) ===
cat /sys/fs/cgroup/cpu/cpu.cfs_quota_us  # 除以 cfs_period_us; cgroup v2 用 cpu.max

# === 冒烟(约 6 min 全过一遍) ===
cd 消融实验/ && python3 run_ablation.py --smoke && cd ..
cd 多算法对比/ && python3 run_comparison.py --smoke && cd ..
cd 优化方案对比（平面、纵断面联合协同优化）/ && python3 run_twostage.py --smoke && python3 run_joint.py --smoke && cd ..
cd 敏感性分析（平、纵联合，重优化）/ && python3 run_reopt.py --smoke && cd ..

# === 全量: 三阶段一键编排(~5.6 h 墙钟, 16 核配额, ~11 进程上限) ===
cd 消融实验/ && setsid nohup python3 -u run_ablation.py > ablation.log 2>&1 < /dev/null & AB=$!
cd ../多算法对比/ && setsid nohup python3 -u run_comparison.py > comparison.log 2>&1 < /dev/null & CP=$!
cd .. && setsid nohup bash run_all_stages.sh $CP $AB > run_all_stages.log 2>&1 < /dev/null &
tail -f run_all_stages.log
```

`run_all_stages.sh` 会在 Stage 1 结束后自动出图，并依次启动 Stage 2（run_joint + run_twostage）与 Stage 3（run_reopt），每阶段结束都立即生成该实验的 `figures/` 与 `tables/`。

全部跑完后，各文件夹 `figures/` 内即为论文用图（png+pdf），`tables/` 内为表格（csv+md）。
