# 项目说明与迁移运行指南

高速公路立体线形优化实验（IJS + 熵权法）。本文档用于把项目迁移到另一台设备后**快速搭好环境并运行**，含尚未跑过的实验的启动方法与预估耗时。

---

## 一、项目概述

以广州北环高速实测数据为例，用改进水母搜索算法（IJS）+ 熵权法标量化，做高速公路平纵断面立体线形优化。共 4 个实验，各在独立文件夹：

| 实验 | 文件夹 | 主运行脚本 | 作用 |
|---|---|---|---|
| 消融实验 | `消融实验/` | `run_ablation.py` | IJS 三改进组件（Tent/Levy/DE）消融，5 变体×30 次 |
| 多算法对比 | `多算法对比/` | `run_comparison.py` | IJS vs NSGA-II/GA/PSO/GWO/JS，3 规模×30 次 |
| 优化方案对比（平纵联合协同） | `优化方案对比（平面、纵断面联合协同优化）/` | `run_joint.py`、`run_twostage.py` | 平纵三维联合协同优化；两阶段对照 |
| 敏感性分析（平纵联合，重优化） | `敏感性分析（平、纵联合，重优化）/` | `run_reopt.py` | 每采样点重新优化线形的敏感性分析 |

`数据/` 为共享数据源；`实验设计方案2（…）.docx` 为总体设计说明。

**四实验统一口径**（已对齐，迁移后无需改）：
- 数据源：各实验 `data_loader.py` 均指向 `实验/数据/数据.xlsx`（相对本文件夹上一级定位，迁移不受绝对路径影响）。
- 桥隧：桥梁 7.8 km、隧道 2.7 km；桥 886.69 万元/km、隧 27000 万元/km；基准里程 L_ref=22.342811 km（取自 `数据/北环高速现状桥梁隧道统计.xlsx`）。
- 能耗：全生命周期货币量（亿元）＝单日能耗 × 365 × 30 年 5% 等额年金现值，不乘标定系数。
- 成本、能耗单位均为**亿元**。

---

## 二、环境搭建（迁移第一步）

1. 安装 Python 3.10+（开发用 3.13）。
2. 在 `实验/` 目录下安装依赖：
   ```bash
   pip install -r requirements.txt
   ```
   依赖：numpy、scipy、pandas、matplotlib、openpyxl（读 xlsx）、python-docx（可选，仅生成 docx 用）。
3. （可选）安装 pandoc —— 仅当需要用 `build_docx.py` 把 md 分析文档转带高亮的 docx 时。绘图/计算不需要。

> 迁移要点：只要保持 `实验/` 文件夹整体结构不变（各实验文件夹 + `数据/` 在同一层），数据路径为相对定位，**换设备/换绝对路径都不用改代码**。

---

## 三、目录结构

```
实验/
├── 数据/                         # 共享数据源(勿改名/移动)
│   ├── 数据.xlsx                 # 北环高速GPS轨迹(14018点)
│   └── 北环高速现状桥梁隧道统计.xlsx
├── requirements.txt
├── 项目说明与迁移运行指南.md      # 本文件
├── 消融实验/
├── 多算法对比/
├── 优化方案对比（平面、纵断面联合协同优化）/
└── 敏感性分析（平、纵联合，重优化）/
```

每个实验文件夹的通用结构：
```
<实验>/
├── run_*.py          # 主运行脚本(生成 results/*.json)
├── make_outputs.py   # 由 results 生成 figures/ 与 tables/
├── params.py         # 参数(pop=200, iter=500, 桥隧/能耗口径)
├── objective*.py     # 目标函数(成本C/能耗E/约束)
├── algorithms.py     # IJS 及变体
├── data_loader.py    # 读 数据/数据.xlsx
├── README.md         # 该实验的说明
├── results/          # 运行输出的 json(迁移可保留或删除后重跑)
├── figures/  tables/ # 图表输出
└── <实验>说明与分析总结.md / .docx
```

---

## 四、各实验运行方法与预估耗时

> 所有脚本均在各自实验文件夹内运行（`cd` 进去即可），无需设环境变量。
> 统一参数: pop=200, iter=500, n_runs=30（消融/多算法）。

### 4.1 消融实验

```bash
cd 消融实验/
python3 run_ablation.py            # 正式跑
python3 run_ablation.py --smoke    # 冒烟测试(iter=5, 验证管线)
```

- 5 变体（JS / JS+Tent / JS+Levy / JS+DE / V5_IJS）× 30 次独立运行
- 维度: 纵断面 ~224 维（100m步长）
- 预估耗时: 约 20–40 min（单核串行；视CPU频率）
- 输出: `results/ablation_results.json`

跑完后生成图表:
```bash
python3 make_outputs.py
```

### 4.2 多算法对比

```bash
cd 多算法对比/
python3 run_comparison.py
python3 run_comparison.py --smoke   # 冒烟
```

- 6 算法（IJS / JS / GA / PSO / GWO / NSGA-II）× 3 规模（small/medium/large）× 30 次
- 预估耗时: 约 1–2 h
- 输出: `results/comparison_results.json`

```bash
python3 make_outputs.py
```

### 4.3 优化方案对比（平纵联合协同优化）

**运行顺序很重要**：先跑两阶段对照，再跑联合优化（`make_outputs.py` 的表 C3 需要两者结果）。

```bash
cd 优化方案对比（平面、纵断面联合协同优化）/

# Step 1: 两阶段对照 (平面单独 → 纵断面单独)
python3 run_twostage.py
# 输出: results/twostage_results.json

# Step 2: 平纵联合协同优化 + Pareto权重扫描
python3 run_joint.py
# 输出: results/joint_results.json (含 M-A/M-B/M-C 三方案 + pareto_sweep 21点)

# Step 3: 生成图表
python3 make_outputs.py
```

- 联合优化维度: **~4494 维**（平面 N_CTRL ≈ 2247 + 纵断面 M_PROF ≈ 2247，10m 步长）
- pop=200, iter=500, Pareto扫描 21 个权重点
- **预估耗时: 8–16 h**（维度极高，是最慢的实验；建议后台 tmux/nohup）
- 建议: `nohup python3 run_joint.py > joint.log 2>&1 &`

### 4.4 敏感性分析（平纵联合，重优化）

```bash
cd 敏感性分析（平、纵联合，重优化）/
python3 run_reopt.py               # 正式全量
python3 run_reopt.py --smoke       # 冒烟测试
```

- 每个采样点都重新跑一次完整 IJS(pop200/iter500) 联合优化
- 采样点 ≈ 207+19 = 226 个（① 6×21=126, ② 6×6=36, ③ 6×6=36, ④ 9, ⑤⑥⑦=7+7+5=19）
- **并行**: 默认 `N_WORKERS=7`（8 核留 1 核余量）；可在 `run_reopt.py` 顶部改
- **预估耗时: 2–4 h**（7 并行，单点 ~3–5 min）
- 输出: `results/reopt_results.json`

```bash
python3 make_outputs.py
```

---

## 五、推荐执行顺序

按依赖和耗时排序的推荐顺序：

```
1. 消融实验          (快, ~30 min)     → make_outputs.py
2. 多算法对比        (中, ~1.5 h)      → make_outputs.py
3. 优化方案对比      (慢, 先 twostage → 再 joint ~12h) → make_outputs.py
4. 敏感性分析(重优化) (慢, ~3 h 并行)  → make_outputs.py
```

实验 1 和 2 可并行跑（互不依赖）。实验 3 和 4 都是联合维度(4494维)，建议在多核机器上分别用 tmux 跑。

---

## 六、冒烟测试（快速验证迁移是否成功）

迁移到新设备后，先用冒烟模式验证全管线无报错：

```bash
cd 消融实验/ && python3 run_ablation.py --smoke && cd ..
cd 多算法对比/ && python3 run_comparison.py --smoke && cd ..
cd 敏感性分析（平、纵联合，重优化）/ && python3 run_reopt.py --smoke && cd ..
```

冒烟用 `iter=5`、少量采样点，每个 1–3 min 跑完。验证无 ImportError / FileNotFoundError / 数据维度不匹配后再正式跑。

优化方案对比的冒烟较特殊（维度太高单次仍需若干分钟），如需验证可只跑 `run_twostage.py`（两阶段对照维度较低，几分钟内完成）。

---

## 七、注意事项

1. **结果均为陈旧(stale)**：当前 `results/` 内的 json 是代码统一前的旧结果，迁移后必须重跑。可安全删除全部 `results/` 内容再跑。
2. **macOS vs Linux**: 代码用 `multiprocessing`（敏感性分析），macOS 默认 `spawn` 启动方式，Linux 为 `fork`。代码已兼容（`_init_worker` 设计），无需修改。
3. **内存**: 联合优化 pop=200 × dim=4494 ≈ 900K floats/个体，加上并行 7 进程，建议至少 16 GB RAM。
4. **随机种子**: 固定种子 `SEED_BASE=20250722`，结果可复现。
5. **图表语言**: 图坐标轴/图例均为英文（SCI论文需要），表格含中文列标题。
6. **数据文件不能改名/移动**: `数据/数据.xlsx` 和 `北环高速现状桥梁隧道统计.xlsx` 必须在 `实验/数据/` 目录下。

---

## 八、快速命令汇总

```bash
# === 一键环境 ===
pip install -r requirements.txt

# === 全量按序执行 (建议用 tmux 分窗) ===
# 窗口1: 消融 + 多算法
cd 消融实验/ && python3 run_ablation.py && python3 make_outputs.py && cd ..
cd 多算法对比/ && python3 run_comparison.py && python3 make_outputs.py && cd ..

# 窗口2: 优化方案对比(最慢)
cd 优化方案对比（平面、纵断面联合协同优化）/
python3 run_twostage.py && python3 run_joint.py && python3 make_outputs.py
cd ..

# 窗口3: 敏感性分析
cd 敏感性分析（平、纵联合，重优化）/
python3 run_reopt.py && python3 make_outputs.py
cd ..
```

全部跑完后，各文件夹 `figures/` 内即为论文用图（png+pdf），`tables/` 内为表格（csv+md）。
