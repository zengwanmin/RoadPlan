# 高速公路立体线形优化实验 —— Python 依赖
# 安装:  pip install -r requirements.txt
# 开发验证版本(numpy 2.x, Python 3.13); 一般 numpy>=1.24 亦可运行。
numpy>=1.24
scipy>=1.10
pandas>=1.5
matplotlib>=3.6
openpyxl>=3.1        # pandas 读取 .xlsx 所需
python-docx>=1.0     # 仅生成 .docx 分析文档时需要(可选)
