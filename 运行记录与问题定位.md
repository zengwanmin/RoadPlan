# 运行记录与问题定位

> 记录 2026-08-05/06 一轮全量重跑的**代码修正、实测结果、在实验③④中定位到的两个
> 模型缺陷及其证据、修复方案，以及修复后的重跑结果**。所有数值均为实测输出，非估计。
> 第三节为「修复前」状态（缺陷定位证据），第六节为「修复后」实测结果与预算公平性核查
> （实验③④均已完成重跑，见 §6.1；④耗时 140.2 min，226/226 采样点）。

---

## 一、本轮修正的内容（重跑前）

| # | 修正 | 起因 | 影响 |
|---|---|---|---|
| 1 | 补装 `openpyxl` | `pandas.read_excel` 缺它，`load_alignment()` 第一步即 `ImportError`，四实验全部无法启动 | 环境 |
| 2 | 平纵步长解耦 `STEP_PLANE_M=400` / `STEP_PROFILE_M=10` | 原代码平面与纵断面同为 10 m，`N_CTRL=2247`；平曲线 R≥400 m 约束下相邻控制点仅允许 2.5 cm 错动，±800 m 走廊带几乎全域不可行 | 实验③④ |
| 3 | CQ 边坡养护项改沿里程积分的面积口径 | 原为逐桩号求和未乘 `dL`，同一条线形在 100 m→10 m 步长下该项凭空放大 10 倍，使实验②跨 P1–P6 的 C/F 不可横向比较 | 全部 |
| 4 | `run_ablation.py` / `run_comparison.py` 补真正的 `--smoke` | 原文档宣传这两个 `--smoke`，但脚本无 argparse，传参被静默忽略而启动全量运行 | 易用性 |
| 5 | 敏感性目录的 `objective_joint.py` 与实验③同步 | 该副本仍是旧的 `N_CTRL=25 / M_PROF=225`，从未跟随迁移，与 README「四实验统一口径」矛盾 | 实验④ |
| 6 | `N_WORKERS` 由硬编码 7 改为自适应 + `--workers` | 原按 8 核机器写死 | 性能 |
| 7 | 并行化 `run_comparison` / `run_joint` | 原全程串行 | 性能 |

### 关于步长解耦的几何依据（修正 2）

相邻平面控制点的横向错动 `A` 在波长 `λ=2Δs` 上产生的曲率半径为 `R = λ²/(4π²A)`。
要满足 R ≥ 400 m（表3.2 极限值）：

| Δs | 允许错动 A | 实测中线自身 Rmin |
|---|---|---|
| 10 m | 2.5 cm | 53.5 m ✗ |
| 100 m | 2.5 m | 302.7 m ✗ |
| 200 m | 10 m | 552.5 m ✓ |
| **400 m** | **40 m** | **525.8 m ✓** |

实测验证（修正前，`N_CTRL=2247`）：`run_joint.py` 生成的初始种群，个体平面线形长度达
**1084 km**（应为 22.5 km）、`Rmin=0`。修正后基线 `Rmin=517 m`、走廊带满幅时 L=39 km。
另：10 m 控制点下实测中线 Rmin 仅 53.5 m，说明样条拟合的是 GPS 噪声而非设计几何；
`数据.xlsx` 的 `R` 列不可用作参照（中位数 0，98.6% 低于 400 m）。

### CQ 口径修正的验证（修正 3）

同一条物理线形（地面线 0.5 km 平滑）在不同桩号步长下的 CQ：

| 步长 | 500 m | 300 m | 100 m | 50 m | 25 m | 10 m |
|---|---|---|---|---|---|---|
| CQ(万元) | 163.48 | 162.19 | 161.24 | 161.40 | 161.34 | 161.31 |

已与步长无关（修正前会相差约 50 倍）。

---

## 二、实测结果（实验①②，有效，无需重跑）

实验①②只使用 `objective.py`（固定平面、仅优化纵断面），**不涉及** `objective_joint.py`，
因此不受第三节两个缺陷影响，结果有效。

### ① 消融实验（表A2，dim=225，pop=200/iter=500/30 次，wC=0.4110）

| 变体 | 最优F | 均值F | 标准差 | 收敛代数 | 运行时间(s) | 相对JS |
|---|---|---|---|---|---|---|
| V1 JS（基线） | 0.5059 | 0.5364 | 0.0237 | 200.3 | 15.20 | — |
| V2 JS+Tent | 0.4733 | 0.4737 | 0.0002 | 54.8 | 15.15 | **+11.70%** |
| V3 JS+Levy | 0.4972 | 0.5376 | 0.0212 | 190.5 | 52.25 | **−0.22%** |
| V4 JS+DE | 0.4793 | 0.4919 | 0.0086 | 108.7 | 33.46 | +8.29% |
| V5 IJS（完整） | 0.4729 | 0.4735 | 0.0002 | 59.1 | 70.00 | +11.73% |

结论：
- **Tent 混沌初始化是唯一的主要贡献者**：F −11.70%，收敛代数 200.3→54.8，标准差从
  2.4e-2 降到 2e-4（两个数量级），且运行时间几乎不变（15.20→15.15 s）。
- **Levy 飞行单独使用是负收益**：F −0.22%（即略差于基线），标准差未改善，
  但运行时间是基线的 3.4 倍。
- DE 有正贡献（+8.29%）但不及 Tent。
- V5 完整 IJS 相对 V2 仅再多 0.03 个百分点，代价是 4.6 倍运行时间。

> 注：运行时间为**单进程串行**测得（本实验刻意不并行），5 个变体条件一致、可直接比较。

### ② 多算法对比（6 规模 × 6 算法 × 30 次 + 各规模 11 点权重扫描）

| 规模 | dim | wC | IJS 最优F | 次优算法 | IJS Friedman 秩 | IJS HV |
|---|---|---|---|---|---|---|
| P1 (500 m) | 45 | 0.783 | 0.5976 | JS (0.5976) 打平 | 1.37 | 0.2032 |
| P2 (300 m) | 75 | 0.642 | 0.5951 | JS (0.5955) | 1.00 | 4.6033 |
| P3 (100 m) | 225 | 0.411 | 0.4729 | JS (0.5059) | 1.00 | 1441.50 |
| P4 (50 m) | 450 | 0.416 | 0.3886 | GWO (0.9976) | 1.00 | 23348.34 |
| P5 (25 m) | 899 | 0.412 | 0.3499 | GWO (0.9541) | 1.00 | 341689.47 |
| P6 (10 m) | 2247 | 0.280 | 0.3304 | GWO (0.7249) | 1.00 | 11828597.39 |

结论：**IJS 的优势随问题维度显现**。P1（dim=45）上 IJS 与 JS 完全打平（同为 0.5976，
Friedman 秩 1.37 即并非严格第一）；从 P2 起 IJS 在全部 30 次运行上稳居第一（秩=1.00）；
P4 起领先次优算法 2.5 倍以上。这比原设计只有 3 个规模时更能支撑"改进组件用于高维
线形优化"的论点，也如实说明了低维时无需 IJS。

> 运行时间按「规模内部严格串行、只跨规模并行（6 进程）」测得，
> 故同一规模内 6 个算法的耗时在同一条件下可比。

---

## 三、实验③定位到的两个缺陷（导致③④结果无效）

### 3.1 实测结果（修复前）

| 方案 | C(亿) | E(亿) | L(km) | Rmin | 纵坡\|i\|max | 超4%站数 | 惩罚(元) |
|---|---|---|---|---|---|---|---|
| M-A 现状 | 8.6705 | 13.2479 | 22.446 | 517 m | 4.05% | 31/2246 | — |
| 联合 M-C (dim=2304) | 8.6092 | 13.1971 | 22.442 | 515 m | 5.12% | 63/2246 | 1.66e8 |
| 两阶段 M-C | 8.2007 | 12.6275 | 21.311 | 554 m | **103.71%** | 98/2246 | **4.30e9** |

表面看两阶段在 C 与 E 上都更优，但其纵坡最大 **103.71%**（46°），另有 54 处相邻坡差
违规，**不是有效的公路设计**；其较低的 C/E 是以约束破坏换取的。惩罚拆解：

| 方案 | 纵坡项 | 坡差项 | 合计 |
|---|---|---|---|
| 联合 M-C | 1.640e8 | 2.036e6 | 1.660e8 |
| 两阶段 M-C | 2.720e9 | 1.575e9 | 4.295e9 |

### 3.2 缺陷一：地面高程"最近邻"近似在平面大幅侧移后失效

`objective_joint.decode_joint` 原用 `cKDTree` 取最近实测中线点的高程作为新桩号点的
地面高程（模块 docstring 已声明此近似成立条件为"走廊带内、地形沿纵向变化为主"）。
两阶段第一阶段为缩短里程把线位侧移了数百米，此条件被破坏：

```
新桩号点到最近实测点的距离: 中位 569.7 m, 最大 736.2 m
最陡处: 桩号 12354 m 与 12363 m（相距 9.5 m）
        最近邻索引 6800 -> 6954（跳了 154 个实测点 ≈ 246 m 路程）
        高程 32.72 -> 22.79 m   =>  虚假纵坡 104%
```

即相邻两个 10 m 桩号取到了**实测路线上相距 246 m 的两个点**的高程。
同一条平面若改用「按里程对应取高程」，地面纵坡最大值为 **6.66%** 而非 104.66%：

| 取高程方式 | 地面纵坡\|i\|max | 中位 |
|---|---|---|
| (A) 最近邻（原用） | 104.66% | 0.63% |
| (B) 里程对应 `np.interp` | **6.66%** | 0.78% |

> 此前曾就 (A)/(B) 做过一次选择并选定保留 (A)（理由：与已文档化口径一致）。当时冷启动
> 与热启动都收敛到 δ≈0，最近邻从未被拉出适用范围，该选择无代价。**平面一旦真正侧移，
> (A) 即失效**——这是后来才出现的新证据。

### 3.3 缺陷二：57 个平面维被 2247 个纵断面维淹没，平面根本不参与寻优

联合方案 L=22.442 km，相对现状 22.446 km 仅缩短 **0.01%**，即 δ≈0、平面完全没动。
原因是 IJS 每次移动同时扰动全部 2304 维并按整体贪婪接受：一个"改善平面但恶化纵断面"
的候选会被直接拒绝，而纵断面在贴地附近已接近最优，任何随机扰动都使其变差，
故平面无法取得进展。收敛曲线证实这是**结构性停滞而非预算不足**：

| iter | 0 | 50 | 100 | 200 | 300 | 400 | 500 |
|---|---|---|---|---|---|---|---|
| F | 2.9147 | 0.3101 | 0.2790 | 0.2690 | 0.2690 | 0.2680 | 0.2679 |

末 100 代仅改善 1.70e-4（占总改善 0.01%），加迭代无用。对照两阶段第一阶段（纯 57 维
平面问题）在 iter=500 时仍在稳定下降（0.9570→0.9494），说明**平面问题本身可解**。

**对照实验（把纵断面也降到 400 m，使两块维度相当）：**

| 方案 | dim | C(亿) | E(亿) | L(km) | Rmin | 惩罚 |
|---|---|---|---|---|---|---|
| 联合 原始 | 2304 (57+2247) | 8.6092 | 13.1971 | 22.442 | 515 m | 1.66e8 |
| 联合 **平衡分块** | 114 (57+57) | 8.4320 | 12.7340 | **21.852** | 979 m | **2.16e4** |

维度一平衡，平面立刻动了（L −2.6%），且惩罚降至近乎为零 —— 三者中唯一几何有效的解。
证实病因是维度悬殊压死搜索，联合模型本身没有问题。

### 3.4 附带发现：2247 个变坡点在工程上本就不成立

原设置令**每 10 m 一个变坡点**，等价于全线 2246 处独立变坡。真实纵断面由数十个变坡点
以等坡段 + 竖曲线连接，这正是相邻坡差（式4.28-4.29）频繁违规的来源。
论文 §4.5.1 的决策变量本身就是"**变坡点**高程"，其个数应远小于评价桩号数。

---

## 四、修复方案（本轮采用）

### 修复一：地面高程改按里程对应取值

`decode_joint` 中以 `np.interp` 按里程比例从实测高程剖面取值，取代 `cKDTree` 最近邻。
依据：无面状 DEM 时，"最近实测点"与"同里程实测点"本是同一近似的两种写法，而后者在
平面侧移后仍然成立（前者不成立，见 3.2）。副作用：单次求值 7.65 ms → 0.80 ms（快 11 倍）。

已实测：在 δ≈0 区（即原方案唯一有效的区域）两者差异为 **C 恰好 0、E ≤ 0.014%**，
故不影响此前任何已验证结论；且新方式给出的地面线更光滑（去除最近邻跳变）。

### 修复二：纵断面决策变量取 400 m 变坡点，目标函数仍在 10 m 桩号积分

- 决策变量：平面 57 个控制点（400 m）+ 纵断面 **57 个变坡点（400 m）**，`dim=114`，两块维度相当；
- 评价：变坡点高程线性插值到 **2247 个 10 m 桩号**，土方、能耗、纵坡、边坡危险度全部
  仍在 10 m 分辨率上积分 —— 不损失评价精度；
- 附带收益：变坡点之间为等坡段，坡度仅在变坡点处改变，与真实纵断面设计一致，
  自然消除 3.4 所述的高频坡差违规。

> 实验①②不受影响（不使用 `objective_joint.py`），其结果与图表保留，无需重跑。

---

## 五、CPU 配额（影响所有耗时估计）

本机 `nproc` 报 124 核，但 cgroup 配额 `cpu.cfs_quota_us/cpu.cfs_period_us = 1600000/100000
= **16 核**`。超配额开进程只会时间片切换而更慢。迁移到新机器**务必先核对真实配额**。

修复前四实验总计约 190 核·h（其中实验④ 160 核·h，占 84%）。修复一使联合类实验
单次求值快 11 倍，修复二把维度从 2304 降到 114，两者叠加后实验③④的成本大幅下降。

---

## 六、修复后结果（实验③④均已完成重跑）与预算公平性核查

### 6.1 修复后实测（对照 3.1 的修复前数值）

| 方案 | C(亿) | E(亿) | L(km) | Rmin | 惩罚 | Q |
|---|---|---|---|---|---|---|
| M-A 现状 | 8.7351 | 13.2408 | 22.446 | — | — | 2.711 |
| 两阶段 M-C | 8.2775 | 12.4187 | 21.311 | 554 m | 0 | 2.659 |
| 联合 M-C (dim=114) | 8.4363 | 12.6185 | 21.688 | 869 m | 4.2e4 | 2.664 |

两个方案惩罚均降到近乎为零（两阶段恰为 0，联合 4.2e4 相对 C_ref≈8.5e8 可忽略），
即修复后**两者都是几何有效的设计**，不再有 3.1 节的 103.71% 假陡坡。里程缩短：
两阶段 −5.06%，联合 −3.38%（原 −0.01%）。图表已用 `make_outputs.py` 重新生成
（表C1–C3、图C1–C7）。

实验④（`run_reopt.py --workers 9`，同一份修复后 `objective_joint.py`，dim=114）
重跑耗时 140.2 min，226/226 采样点全部完成，几何量呈真实非退化变化（如表D3
Rmin 在 416–1150 m 之间、L 在 21.617–21.762 km 之间随采样点变化），
不再是修复前"平面被冻死、全部采样点收敛到同一解"的伪影。

### 6.2 预算公平性核查（新发现，非本轮"两个缺陷"之列，但影响表C3的解读）

表C3 让两阶段（Stage1+Stage2 各一次 IJS，pop=200/iter=500）与联合（单次 IJS，
pop=200/iter=500）在同一 iter 下比较，但**总求值次数不对等**：两阶段 ≈600,400，
联合 ≈300,200（约 2 倍差）。用 `/tmp/fair_budget.py <iter>`（联合，变 iter）与
`/tmp/fair_twostage.py <iter>`（两阶段，变每阶段 iter）按总求值量对齐复测：

| 预算 | 方法 | 总求值量 | C(亿) | E(亿) | L(km) | Rmin(m) | 惩罚 |
|---|---|---|---|---|---|---|---|
| 官方 iter=500 | 两阶段 | ~600,400 | 8.2775 | 12.4187 | 21.311 | 554 | 0 |
| 官方 iter=500 | 联合 | ~300,200 | 8.4363 | 12.6185 | 21.688 | 869 | 4.2e4 |
| **等预算** | 两阶段(iter=500) | ~600,400 | 8.2775 | 12.4187 | 21.311 | **554** | 0 |
| **等预算** | 联合(iter=1000) | ~600,200 | 8.2966 | 12.4147 | 21.338 | **1295** | 7.4e4 |
| **2×预算** | 两阶段(每阶段iter=1000) | ~1,200,400 | **8.1074** | **12.1483** | **20.861** | 452 | 0 |
| **2×预算** | 联合(iter=2000) | ~1,200,200 | 8.1992 | 12.2563 | 21.078 | 1311 | 8.5e4 |

结论：
- **等预算下基本打平**（C/E/L 差距 <0.3%），联合的 Rmin 是两阶段的 **2.3 倍**（1295m vs 554m）。
- **2×预算下两阶段反而拉开优势**（C/E/L 领先联合 0.9–1.1%）——但这不是"两阶段被少算了"
  的假象：两阶段的第一阶段目标只要求 R≥400m 可行、不奖励富余量，预算越多越会把半径压向
  约束边界本身（Rmin 554m→452m，随预算增加下降）；联合模型平面与成本/能耗协同寻优，
  Rmin 反而随预算增加更安全（869m→1295m→1311m）。
- 净结论：**两阶段更省成本、联合几何富余度更高**，这一权衡在任意预算下都存在，
  不是预算不对等造成的假象。用户决定（见对话记录）：**官方表C3 仍按 pop=200/iter=500
  （用户指定参数）呈现，不改动数值**，仅在 `表C3_现状_两阶段_联合协同_三方案对比表.md`
  末尾追加本节结论作为附注（`make_outputs.py` 的 `_append_c3_budget_note()`）。

---

## 七、归档

- 旧的（本轮之前提交在仓库里的）`results/`、`figures/`、`tables/` 共 60 个文件已移至
  `/tmp/roadplan_stale/`。它们产自 `dim=250`、桥隧费=0、含 `k_E` 反向标定的旧口径，
  与当前代码不可比。
- 修复前的实验③产物（`joint_results.json` / `twostage_results.json` 及图表）与
  实验④部分进度（96/226 采样点日志 `reopt_partial_86of226.log`）亦归档于同处，
  作为本文档第三节结论的原始依据。
- 四份 `<实验>说明与分析总结.md/.docx` 仍基于最早的旧结果撰写，**需按新结果重写**；
  其中敏感性那份仍在引用早已从代码中移除的 `β=4.2948×10⁷` 与 `k_E=0.6792`。
